[package]
name = "aoc"
version = "0.1.0"
edition = "2024"

[dependencies]
itertools = "0.14.0"
reqwest = { version = "0.12.25", features = ["blocking", "cookies"] }

[dev-dependencies]
rstest = "0.26.1"
